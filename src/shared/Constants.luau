-- Tunable game parameters. Data-only: balancing edits this table, never code.
local Constants = {
	MinPlayers = 1,
	CountdownSeconds = 10,
	RoundDuration = 60,
	RoundRotation = { "FloorFall", "HotPotato" },
}

return Constants
