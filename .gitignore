# Build outputs
/build/

# Rojo sourcemap (regenerated by `rojo serve` / `rojo build`)
/sourcemap.json

# Roblox Studio lock files
*.rbxl.lock
*.rbxlx.lock

# selene Roblox API std (regenerated locally and in CI via `selene generate-roblox-std`)
/roblox.yml
