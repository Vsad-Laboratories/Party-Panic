# Party Panic — Vision & Session Context

> Handoff document for any AI session. Read this first, then `docs/GDD.md` (locked decisions + roadmap), then `~/RULES.md` + `~/MastersEngineer.md` (behavior + code rules).

## Vision

Build the simplest round-based party royale on Roblox that kids 9–13 replay daily and invite friends to — shipped and maintained by a solo operator with a professional AI-agent workflow, where **content updates are data-only and ship weekly**.

Success looks like: D1 retention above Roblox genre average, session length 10+ min, organic invites (referral cosmetics), and a repo where a new minigame never risks breaking the existing game.

## Pillars (every decision checks against these)

1. **Content is data, one DIRECTORY per minigame — no system in one file** (Operator rule, 2026-09-24) — `src/server/rounds/<Game>/` holds `init.luau` (lifecycle only), `config.luau` (pure data table), plus one file per subsystem (`floor.luau`, `players.luau`, `potato.luau`, …); new round/map/cosmetic = data + a new directory, zero edits to the core loop. Same rule everywhere: frame files live in `src/server/core/`, UI modules in `src/client/ui/`.
2. **The frame is fixed** — lobby → vote → round state machine → rewards. Code changes are rare; data changes are weekly.
3. **Nothing merges red** — CI gate: `stylua --check` → `selene` → `rojo build`. AI PRs must pass before human review.
4. **Anti-spaghetti** — RULES.md hard rules 11/12: codebase trends toward spaghetti-fication → STOP, ask Operator; no fluff additions ever.
5. **Minimal, portable, idempotent** — every line justifies itself; `~` paths; re-runs safe.

## Architecture

```
Party Panic (Roblox place, built by Rojo from this repo)
├── src/server/      Bootstrap.server.luau + core/ (frame: LobbyMachine, RoundRegistry)
│   ├── core/        the fixed frame (Pillar 2) — rare edits
│   └── rounds/<Game>/  ONE DIRECTORY PER GAME (Pillar 1):
│              init.luau = RoundContract lifecycle   config.luau = pure data
│              + subsystem files (floor.luau, players.luau, potato.luau, …)
├── src/client/      init.client.luau + ui/ (LobbyUI, future widgets) + input, spectate later
├── src/shared/      round interface, contracts, typed payloads
├── assets/          .obj meshes + textures, versioned (AI-gen, low-poly <5k tris, single palette)
├── default.project.json   Rojo: repo → place mapping
├── rokit.toml              pinned: rojo 7.7.0, selene 0.31.0, stylua 2.5.2
├── docs/GDD.md             locked decisions + roadmap
├── Vision.md               this file
└── .github/workflows/ci.yml  stylua --check → selene → rojo build → PartyPanic.rbxlx artifact
```

- **DataModel mapping:** `ServerScriptService.Server` ← `src/server` (Folder: `Bootstrap.server.luau` boot Script + ModuleScript siblings), `StarterPlayer.StarterPlayerScripts.Client` ← `src/client` (LocalScript), `ReplicatedStorage.Shared` ← `src/shared` (ModuleScripts, live).
- **Round contract (written: `src/shared/RoundContract.luau`; conformance via annotations + data-driven name lookup in `src/server/RoundRegistry.luau`):** each round module exposes lifecycle functions the state machine calls (setup → play → cleanup); rounds never touch each other — this is what keeps AI-generated rounds safe to merge. Games live as **directories**: `src/server/rounds/<Game>/` (Rojo init rule makes the directory itself the round ModuleScript, subsystem files become its children — registry lookup unchanged).

## Stack & workflow (locked)

| Layer | Choice | Note |
|---|---|---|
| Repo | github.com/Vsad-Laboratories/Party-Panic, `main` | git-push-driven; every push = trackable change |
| Local Studio | **Vinegar on HP Pavilion 14 SleekBook — VERIFIED 2026-09-23: Studio launches** (fix: `[studio] renderer = "Vulkan"` in Vinegar config; earlier "silicon ceiling" verdict was wrong) | Linux box; no official browser Studio exists. CI artifact `PartyPanic.rbxlx` remains the fallback + publish source |
| Agent multiplexer | herdr (v0.9.1 installed) | replaced OpenChamber — rejected 2026-09-23 |
| Feature coding | Kilo Code | works in-repo against `src/` |
| Autonomous tasks | Jules | GitHub issues → PRs; must pass CI first |
| 3D assets | Meshy / Tripo / Rodin | low-poly, <5k tris, single palette texture |
| Agent roles/skills | herdr fleet plan (2026-09-23): **Control Agent** (talk/discuss/decide, like this session) + role agents — UI designer, Backend, Code Reviewer; Roblox/Luau skills installed via skills.sh | under construction |

Game loop: lobby → vote → 60–90 s round → elimination/scoring → coins → repeat; winner wears crown in lobby.

Monetization (locked): cosmetics + battle pass + power/perk gamepasses + products; ads later. Recorded warning: power gamepasses to 9–13 risk review/retention burn — cap power, lead with cosmetics.

## Current state (update on every session end)

- ✅ Repo bootstrapped and **public** → GitHub Actions free. Pushes: `eadc3c7` bootstrap → `8f4f285` Vision.md → `b80fd80` CI fix.
- ✅ **CI verified green** (run 35851129161, 9 s): `rokit install --no-trust-check` (official CI flag — trust prompts are interactive-only) → selene std → stylua --check → selene → rojo build → `PartyPanic.rbxlx` artifact uploaded. Local gates also green (0 lint errors, instances verified in place file).
- ✅ **Vinegar WORKS (2026-09-23): reinstalled 1.9.4 → Roblox Studio launches to home screen.** Fix: `~/.var/app/org.vinegarhq.Vinegar/config/vinegar/config.toml` → `[studio] renderer = "Vulkan"` (YouTube-tutorial-sourced). **My earlier "silicon ceiling" verdict was wrong** — `intel_hasvk` ICD exposes Ivy Bridge HD 4000 at Vulkan 1.3.354 (read from ICD manifests); the first-run clientcrash (RBXCRASH-Undefined, exit 67, DXVK "No adapters found") did not recur after clean reinstall + renderer setting — original cause was the botched first-run install. Sober 1.7.1 kept. Logs: `~/.var/app/org.vinegarhq.Vinegar/cache/vinegar/logs/`.
- ✅ **Path A locked (2026-09-23): no-Studio loop** — push → CI build → Open Cloud publish (`POST https://apis.roblox.com/universes/v1/{universeId}/places/{placeId}/versions?versionType=Published`, scope `universe-places:write`, .rbxlx as data-binary, official docs verified) → playtest in Sober. CI `publish` job exists but **skips until repo is configured**: secret `ROBLOX_API_KEY` + vars `ROBLOX_UNIVERSE_ID`, `ROBLOX_PLACE_ID`.
- ✅ **Repo ARMED (2026-09-23):** vars `ROBLOX_UNIVERSE_ID=10767732474`, `ROBLOX_PLACE_ID=118657294629389` + secret `ROBLOX_API_KEY` all set. Identity verified via Open Cloud `GET /cloud/v2/universes/…` with the key → displayName **"Party Panic"**, PRIVATE, root place matches (donation place untouched). Publish job now fires on every push to `main`. ⚠️ Dashboard flags worth reviewing (target = 9–13, PRD): `voiceChatEnabled: true`, `ageRating: AGE_RATING_13_PLUS`.
- ❌ **PI-Desktop / PI Agent: REJECTED (2026-09-23) — Operator decision: "only Herdr stays."** PI Agent deleted completely (`~/.local/bin/pi` launcher → `~/.pi/` data, 157 MB; verified 4/4 gone). vastsa/PI-Desktop evaluated (local-first, Electron+Rust, A2A protocol) — not installed, out of stack.
- ✅ **First Open Cloud publish SUCCEEDED (2026-09-23):** `{"versionNumber": 4}` after granting universe-places Write — push → CI build → live place version proven end-to-end (run 35881390699, build ✓ publish ✓).
- ✅ **Fleet LIVE (2026-09-23):** Control relocated into herdr (`HERDR_ENV=1`, pane w1:p2, tab w1:t2). Fleet: worktrees `~/Dev/PartyPanic-{backend,client,reviewer}` on branches `agent/backend-s1-frame` / `agent/client-ui-shell` / `agent/reviewer-checklist`; tabs+agents: **backend** (kilo, w1:p3), **client** (kilo, w1:p4), **reviewer** (opencode, w1:p5). Onboarding prompts dispatched from `docs/agents/*` charters. Permission policy: Operator chose **Always allow `/home/vsad/*`** (agents must read `~/RULES.md`). **PR #1 (`docs/REVIEW.md` checklist) APPROVED by Control + MERGED — full protocol proven: charter → work → gates → PR → CI green → Control merge.** PR #2 + PR #3 merged — **by Operator with Reviewer REQUEST CHANGES open** (fix-forward; Reviewer flagged the deviation for record — merges must return to Reviewer APPROVE → Control once fix cycle lands). PR #4 = backend fixes: 6/6 original findings verified resolved (byte-level gate-evidence match), 1 new guard finding in flight; client re-dispatched for its 7 findings. ⚠️ Ops gotchas: (1) kilo ignores opencode config (banner, harmless — prompts are self-contained); (2) kilo's first-run welcome screen can silently swallow a prompt — verify `state_change_seq`/read before assuming delivery; (3) `gh run watch` exit 0 does NOT mean run success — read job results; (4) model management: backend pinned `-m kilo/poolside/laguna-s-2.1:free` (perfect record after gateway `Auto` 503'd twice); **client model is Operator-managed** (Laguna rate-limited → Operator set `Nex AGI/Nex-N2.5-Pro`, may switch again — do not re-pin client); restart recipe: `/exit` → `herdr agent start … -- -m <model>` (fresh session — resend onboarding); (5) backlog: no CI type-check gate yet — `--!strict` annotations not machine-enforced.
- 📋 **Advisory review adopted (2026-09-24):** external Roblox analyst validated the concept → PRD §2 rewritten as staged funnel launch gates (first-round ≤ 30 s, completion, D1 20/30, D7, rematch, crash + new events `first_round_start`/`first_round_complete`/`rematch`), FR2/FR5 extended, FR11 rematch + FR12 micro-awards added; GDD gains elimination engagement, status cosmetics, refined update rhythm, 8-round backlog (Hot Potato … Team Tangle), monetization cautions (no unpreviewed random paid rewards), 100k-MAU revenue scenarios table, and **battle pass gated on D7 cohort proof (replaces week-8 build)**; Operator backlog recorded: **player minigame voting** (post-v1, same gate). Idea.md synced. Operator's revenue framing: 100k MAU × 5–300 R$/player ≈ $1.3k–$80k/mo developer cash (70% share, DevEx ≈ $0.0038/R$).
- ✅ **Sprint 2 complete (2026-09-24): PRs #5–#9 merged through the full protocol** (Reviewer verdict every merge — no operator overrides this round): #6 client fixes 7/7 · **#7 Floor Fall + lobby state sync** (3 review rounds — fixed: participants never placed on floor, ghost-leaver wins, non-zeroed placement velocity tunneling through 1-stud tiles, solo instant-win → gated `startingCount > 1`) · #8 mock rework (emit-on-change, teardown residue gone) · **#9 LobbyUI wired to the real feed — MockState deleted.** **Playable end-to-end on main + published:** lobby (live player list/countdown) → Floor Fall (procedural tile grid, wave drops, Y-threshold elimination, last-standing, timeout fallback, solo-safe) → results/winner crown → repeat; state typed in `src/shared/LobbyState.luau`, published via `LobbyStateEvent` on every FSM transition + tick + join/leave. New: `docs/PLAYTEST.md` (operator checklist vs PRD §2 gates). ⚠️ **Client-model incident:** Operator's `Nex-N2.5-Pro` timed out 3× mid-turn (503 "timed out while sending the response"; 7m41s thought @ 7.1 t/s) → client session restarted on **Laguna** (`/exit` → `start … -- -m kilo/poolside/laguna-s-2.1:free` + full onboarding resend) — Laguna rate-limit blips self-retry fine; **Operator may switch client back to Nex.** Crash-resilience rule adopted: instruct agents to commit+push after every step so stream deaths can't lose work.
- 📋 **Specialist round 2 (2026-09-24): monetization model adopted, rate corrected** — DevEx official **$0.0035/R$** ($350/100k) replaces the unverified $0.0038 (GDD revenue section rewritten: 100k-MAU rows recomputed + DAU×ARPU table 1K–20K DAU + levers: conversion 2–5%, Premium payouts 10–15%, D1<35%/D7<10% churn warning reconciled with PRD staged gates). Maps input: validates runtime-built procedural arenas (Floor Fall = his "dynamic platforms" example); ideas logged — obstacle spawners → round backlog, spectator state → post-v1, teleporter-per-instance hub architecture **rejected** (single-place rotation is the locked frame). Fleet: **map agent live**, all 5 agents grouped under OpenVised/w1 after stray-workspace fix (map tab `w1:t8`).
- 🛑 **Session end (2026-09-24, Operator EOD): fleet stopped** — backend idle, reviewer done, map idle, client TUI exited (dead stream, ignored esc → SIGINT; restart via `herdr agent start client --kind kilo --pane <pane> -- -m kilo/poolside/laguna-s-2.1:free`). **PR #12 LobbyHub = REQUEST CHANGES, 6 findings** (top two: [C2] Bootstrap diff +2/−8 — map agent DELETED the lobby FSM boot (LobbyMachine require/new/start) instead of adding 2 lines beside it; [C6] `require(...Server.maps.LobbyHub.init)` = nil — Rojo consumes init.luau into the LobbyHub ModuleScript itself, no `.init` child; fix path to `maps.LobbyHub`; findings 3–6 in PR comment). Next session: map fixes findings → reviewer re-verdict → merge → publish → backend lobby-placement task unblocks (spawnPoints contract now exists in `LobbyHub/config.luau`). PR #11 (UI root-cause fix) merged + published `ac63358`. **Free-tier rate-limit logjam:** Poolside hit attempt #8, Qwen `qwen/qwen3.8-27b:free` attempt #10 (13-min backoff) — **Nemotron 3 Super (free) was the pool that broke through and shipped all of #12; map agent stays on Nemotron.** Fleet sidebar grouping fixed: all 5 agents under OpenVised/w1 (map = tab t8, stray w2 closed).
- 🔐 **API key incident resolved (2026-09-25) + new standing rules.** Leak vector evidence: the key NEVER entered git — plaintext-key grep across all refs/history/`.github`/`docs` = zero, workflow uses `secrets.ROBLOX_API_KEY` only, CI logs/PR bodies clean. It existed only as local plaintext `Roblox API Key.md` (created 2026-09-23 20:25, untracked, never committed). Operator rotated; **new key set in `ROBLOX_API_KEY` secret 2026-09-25T07:58Z**. Standing rules: (1) keys NEVER stored in the repo directory — `gh secret set` + password manager only; (2) `Roblox API Key.md` gitignored (accidental `git add -A` to the public repo was the near-miss); (3) Roblox dashboard key = expiry ≤90 days + publish-only scopes; (4) Operator confirms the exact Roblox notice wording — "used from unrecognized IP" may be GitHub-Actions IP usage (CI publishes from US IPs since 09-23), not an actual exfil.
- 📋 **Specialist round 3 (2026-09-25) adopted.** Retention reality: platform median D1 10.3% / D7 1.6%, "good" floor 20% → **staged PRD gates 20/30 confirmed**, 35% D1 = flawless-FTUE stretch. Cosmetics priority for 9–13: **trails/auras (lobby flex + free advertising) > emotes/soundboards (social) > outfits (lowest — kids keep their own avatar)** → shop build order. **Creator Rewards**: qualifying "active spender" ($9.99+/60d) + 10+ min session + first-3-stops = flat reward → our ≥8-min session sits BELOW the 10-min threshold → adopt "one more round" bridge loops (lobby vote, quick rematch) targeting **10–12 min** (design lever; PRD ≥8 stays the floor). DevEx facts: 30k Earned Robux minimum, age 13+, Premium NO LONGER required, under-18 → parent/guardian legal name + W-9/W-8 on first cashout, earned-vs-unearned scrutiny. Rate: he quotes $0.0038 — we standardize official **$0.0035** (30k → $105). His open Qs answered: monetization loops = straight cosmetics storefront (no gacha — PRD bans unpreviewed random paid rewards), battle pass gated on D7, coins = free progression; round length = 5 rounds, first round ≤30s join→play, rounds ~60–90s, session ≥8 min.
- 📋 **Specialist round 4 (2026-09-25): session-bridge mechanics adopted** (8→10–12 min Creator Rewards gap): (1) active spectator mini-games (cosmetic/chaotic only — water balloons, hazard triggers, no competitive advantage), (2) 30-second lobby flex arena — podium for round winners + emote/soundboard stage between rounds (+~2 min/session over 5 rounds), (3) daily play-streak coin multiplier / "Streak Protect" if all 5 rounds completed (loss-averse retention hook), (4) roll-over micro-quests on lobby UI ("1 more round to finish X"), (5) seamless map-vote overlay at Round 5 end — no black-screen leave moment, (6) perf = retention: StreamingEnabled + part pooling/recycling for low-end mobile. Store in GDD backlog; sequence post-core-playtest. Rojo plugin = the **"Rojo 7"** tile in Store (matches CLI 7.7.0; ignore Rojo 6/0.5/0.4/Boatly knockoffs).
- ⚠️ **Publish blocked on key SCOPE (2026-09-25):** new key = valid (cloud v2 GET 200) but `"insufficient scopes"` on `universes/v1 places/versions publish` → Operator must edit key in dashboard → add **publish place version** permission → Control reruns run 36111393998. Timeline: run 36111393998 = the #13 publish (build green, publish 401/401/403 across attempts; first two attempts were the invalid pre-fix key value + propagation).
- 🎨 **Theme LOCKED (Operator + specialist, 2026-09-25): "Astro-Arcade Punk"** — cozy low-poly sci-fi, chunky geometry, soft neon. **3-color palette (binding for all maps + UI):** base = matte slate blue / deep cosmic navy (`Color3.fromRGB(46,58,89)` / `fromRGB(35,43,69)`), accent = soft teal / pastel cyan (`fromRGB(95,227,211)`), highlight = electric neon pink `fromRGB(255,63,164)` / canary yellow `fromRGB(255,225,77)` reserved for interactive zones/booths/rare drops. Low-poly over texture, no lens flares (30 fps low-end rule). Lobby mockup zones accepted (Operator's specialist visualized it): **Storefront Avenue** (world-modeled shop booths = Zero-FOMO storefront), **Battle Pass Grotto**, **Map Voting circle + STATUS screen**, **Winner Podium**, **AFK & Coin Obby Arena**. Specialist's "3D Interactive Wishlist Station" (pin 3 cosmetics, DataStore, social flex) = **accepted to backlog** (needs persistence beyond v1 coins).
- 📖 **`docs/DESIGN.md` = design bible** (specialist round 5: lobby layout facts binding for Phase A, per-minigame centerpieces + 3 map concepts = Phase A/B reference, Vector3 offer pending per-map dispatch).
- 📦 **Asset registry live: `docs/ASSETS.md`** (batch 1: 9 audios, 10 models, VFX/trail/aura packs, 2 UI icons — Operator's Creator Store haul 2026-09-25). Ingest rules binding: community scripts **never run** (load unparented → strip Script/LocalScript/ModuleScript → parent), IDs referenced only via map `config.luau`, free assets only. Open flag: ID `6121196773` listed twice (conveyor + bench) — correction requested.
- 🚫 **Operator UX doctrine (2026-09-25, supersedes auto-loop):** **NO automatic round join/start** — lobby = free-roam hub; players explore and start rounds deliberately (trigger design pending — Operator's example image did not arrive). **Delete all existing minigame scaffolding** (FloorFall, HotPotato, rotation, countdown) and rebuild maps one at a time **starting with Lobby**. **UI = complete redesign** under the new **ui agent** (kilo @ `w1:p9`, `~/Dev/PartyPanic-ui`, charter `docs/agents/UI.md`, owns `src/client/ui/**` — client agent keeps gameplay client). "Bad aura effect" diagnosed = old `LobbyUI` `CrownArea` (yellow center-screen frame) — dies with the UI teardown. PRD round-cycle section needs amendment once the start trigger is confirmed.
- 🗺️ **Map build phase doctrine (Operator, 2026-09-25):** Phase A = pure-code visual craft — map agent builds each map with precision palettes/colour/landmark quality, **geometry only, no gameplay code**; Phase B = functionality wiring (round systems consume the geometry); Phase C = reviewer audit → fixes applied. Operator = design supply (ask him for maps/objects/interactive objects).
- ⏳ **Rojo Studio plugin NOT installed yet** (manual step in Studio): https://www.roblox.com/library/96353449041057/Rojo-7-5-1 → then `rojo serve` in `~/Dev/Party Panic` + Connect = local hot-sync loop.
- Known CI noise (not bugs): `actions/checkout@v4` / `upload-artifact@v4` Node 20 deprecation warning (forced onto Node 24, harmless); `ubuntu-latest` → Ubuntu 26 migration notice (Oct 2026).

## Next steps (in order)

1. Operator setup for Path A — **Studio now runs on the SleekBook, no borrowing needed** (web Create still can't make experiences; Open Cloud has no create endpoint — creation is Studio-only): File → New → Baseplate → File → **Publish to Roblox As…** → name "Party Panic", keep Private (avoids overwriting `Owner_Vsad's Place`) → dashboard: Universe ID (thumbnail ⋯ menu) + Place ID (Places tab → URL) → API key (create.roblox.com/dashboard/credentials), **universe-places → Write**, bound to the game → repo secret `ROBLOX_API_KEY` + vars `ROBLOX_UNIVERSE_ID`/`ROBLOX_PLACE_ID` → push → confirm `versionNumber` in publish step → playtest in Sober.
2. ~~Sprint 1–2: lobby + Floor Fall round~~ **DONE (2026-09-24)** — playable end-to-end, published. Next (weeks 3–5): round rotation from registry + round 2 (Hot Potato), then map voting protocol.
3. Operator: playtest per `docs/PLAYTEST.md`, then the manual steps (Rojo plugin, dashboard flags, **API key rotation**).
